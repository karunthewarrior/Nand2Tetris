/**
 * And gate: 
 * out = 1 if (a == 1 and b == 1 and c == 1)
 *       0 otherwise
 */

CHIP And3 {
    IN a, b, c;
    OUT out;

    PARTS:
    // Put your code here:
    And (a=a,b=b,out=and1);
    And (a=and1,b=c,out=out);
}
